@implements IAsyncDisposable
@namespace KillaCore.Blazor.FileUpload.Components

@code {
    // This component renders nothing visible.
    // It provides State and Logic to its parent via the 'Transfers' property.
}